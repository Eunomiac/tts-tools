{
  "name": "@tts-tools/gateway-client",
  "description": "Client library for the TTS Tools local gateway (register, fan-out, proxied executeLua).",
  "version": "0.2.0",
  "author": "Eunomiac",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.build.json"
  },
  "devDependencies": {
    "@types/node": "~20.17.19",
    "rimraf": "~6.0.1",
    "typescript": "5.7.3"
  },
  "files": [
    "/dist",
    "/src"
  ]
}

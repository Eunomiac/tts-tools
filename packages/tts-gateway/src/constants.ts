/** TTS → editor events (External Editor API). */
export const TTS_EDITOR_PORT = 39998;

/** Client → TTS commands. */
export const TTS_COMMAND_PORT = 39999;

/** Gateway control / register port (NDJSON over TCP). */
export const GATEWAY_CONTROL_PORT = 39997;

export const HEARTBEAT_INTERVAL_MS = 2000;
export const HEARTBEAT_TIMEOUT_MS = 8000;
